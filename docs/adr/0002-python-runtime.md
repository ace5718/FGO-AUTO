# Python 3.11+ for the first implementation

FGO-AUTO v1 is a Windows-oriented CLI using Python 3.11+ for screen capture, template matching, and state machines. Packaging (e.g. PyInstaller) is deferred.
# Fixed BlueStacks display preset (1920x1080)

FGO-AUTO targets TW FGO on BlueStacks 5 via window capture. Image matching assumes Display preset 1920x1080 (no scaling in v1).

Runs verify the game window matches this preset before capture. Multi-preset packs and automatic scaling are deferred.
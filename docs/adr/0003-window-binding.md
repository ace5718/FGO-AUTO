# Window binding via title rule, optional Window pick

Each Run binds one BlueStacks window using a Window title rule in RunConfig. Multiple matches refuse to start until Operator uses `fgo-auto window-pick` or narrows the rule.